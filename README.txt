سامانه صابر | SABER Management Site Windows

این بسته برای قرار دادن نسخه فعلی سایت سامانه صابر در GitHub آماده شده است.

محتویات:
1) Saber Management Site Windows.html
   - فایل HTML اصلی، بدون تغییر در محتوای آن.
2) assets/saber-logo.png
   - لوگوی ارسالی برای استفاده به عنوان لوگوی پروژه.
3) README.txt
   - راهنمای این بسته.

نکته مهم:
فایل HTML اصلی عمداً دستکاری یا بازنویسی نشده است. لوگو به صورت فایل مستقل داخل بسته قرار گرفته تا نسخه اصلی HTML حفظ شود.

روش استفاده در GitHub:
- اگر سایت GitHub Pages است، نام فایل اصلی را در صورت نیاز به index.html تغییر دهید.
- پوشه assets را نیز همراه فایل HTML در همان Repository نگه دارید.
- برای جلوگیری از خراب شدن لینک‌ها، ساختار پوشه assets را تغییر ندهید مگر اینکه ارجاع‌های HTML نیز عمداً تغییر کنند.

نسخه Windows:
این بسته با عنوان Windows آماده شده و فایل HTML موجود در آن همان فایل دریافت‌شده است.
